import { RouteLoadingState } from '../../components/route-loading-state';

export default function NotificationsLoading() {
  return <RouteLoadingState label="Loading notification delivery records…" />;
}
